package theroom;

import data.Model;
import data.Vector3f;

public class EntityZombie extends Entity{

	public EntityZombie(Vector3f position, Vector3f lookingDirection,
			int maxHealth, Model model) {
		super(position, lookingDirection, maxHealth, model);
		// TODO Auto-generated constructor stub
	}

	
}
